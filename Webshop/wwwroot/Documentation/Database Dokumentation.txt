Update-Database 
-> in PM-Console eingeben um Datenbank über Änderungen im Code zu ändern

Scaffold-DbContext "Server=DESKTOP-MJJE09O;Database=LapWebshop; Persist Security Info=True; User ID=SA;Password=123User!;" Microsoft.EntityFrameworkCore.SqlServer -OutputDir Models -Force
-> das wird aber alle Anpassungen in den Models überschreiben (außer in den ViewModels)
-> anwenden wenn man änderungen an der Datenbank macht und diese in den Code übernehmen will